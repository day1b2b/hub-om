# Claude Code 작업 지침

Claude Code는 막연한 자동완성 도구가 아니라 구현 파트너로 사용합니다. 작업을 맡길 때는 범위, 담당자, 참고 문서, 검증 기준을 작게 쪼개서 명확히 줍니다.

## 작업 시작 프로토콜

Claude Code는 모든 작업을 시작하기 전에 아래를 반드시 수행합니다. 이 프로토콜을 생략하지 않습니다.

1. 현재 브랜치를 확인합니다.
2. 현재 브랜치가 `main` 또는 `dev`라면 직접 작업하지 않고 새 작업 브랜치를 만듭니다.
3. 아래 문서를 읽습니다.
   - `docs/collaboration/working-rules.md`
   - `docs/operations/db-write-safety.md`
   - 작업과 관련된 공개 규칙 문서
4. 작업 범위와 수정 금지 항목을 확인합니다.
5. 실제 운영 데이터 수정/삭제가 필요한 작업인지 확인합니다.
6. 열린 결정이 필요한 작업인지 확인합니다.
7. 위 내용을 짧게 보고한 뒤 작업을 시작합니다.

작업 시작 보고 형식:

```text
작업 시작 확인:
- 현재 브랜치:
- 읽은 문서:
- 작업 범위:
- 수정 금지:
- 실제 데이터 영향:
- 열린 결정 가능성:
```

## 작업 전 반드시 읽을 문서

Claude Code는 작업 전에 아래 문서를 먼저 읽어야 합니다.

- `README.md`
- `docs/README.md`
- `docs/collaboration/getting-started.md`
- `docs/collaboration/working-rules.md`
- `docs/collaboration/team-workflow.md`
- `docs/operations/db-write-safety.md`
- `docs/operations/database-runbook.md`

DB, migration, import, 운영 데이터 write-back, 대량 수정 작업이면 아래 문서도 읽습니다.

- `docs/operations/db-write-safety.md`

DB 스키마, 마이그레이션, DB 연결, API 계약, 인증/권한, 배포를 건드리는 작업은 머지 전에 데이터/기술 책임자 검토가 필요합니다.

`main`과 `dev`에는 직접 작업하지 않습니다. 실제 작업은 `feature/YYYYMMDD-작업명`, `fix/YYYYMMDD-작업명`, `docs/YYYYMMDD-작업명` 같은 작업 종류 기반 브랜치에서 하고, 검증 후 `dev`로 합칩니다.

DB, migration, import, 대량 수정, 운영 데이터 write-back 작업은 [docs/operations/db-write-safety.md](docs/operations/db-write-safety.md)를 먼저 읽고 백업 필요성을 확인합니다.

## 확정 앱 스택

- 앱 프레임워크: `Next.js`
- 언어: `TypeScript`
- UI 런타임: `React`
- 호스팅: `Coolify`
- 데이터베이스: `PostgreSQL`
- 배포 빌드: Next.js `standalone`
- 데이터 접근 방식: `repository/interface`
- 외부 원천 adapter와 실제 데이터 예시는 공개 저장소에 커밋하지 않음

Claude Code는 화면 컴포넌트가 원천 데이터나 PostgreSQL 테이블에 직접 의존하게 만들면 안 됩니다. 화면은 표준 operation 타입과 repository interface를 통해 데이터를 받아야 합니다.

## Claude Code 요청 템플릿

대부분의 작업은 아래 형식으로 요청합니다.

```text
작업:
<구체적인 화면/기능/API 동작>을 구현한다.

맥락:
- 담당자: <작업자 | 리뷰어>
- 제품 워크플로우: <운영 목록 | 운영 상세 | 종료회고 | 리포팅 등>
- 관련 파일: <알고 있으면 경로 작성>
- 데이터/API 계약: <비공개 계약 문서 또는 이슈 링크>

제약:
- diff를 작게 유지한다.
- 기존 패턴을 따른다.
- 승인 없이 새 의존성을 추가하지 않는다.
- 이 작업에서 명시하지 않은 DB 스키마는 바꾸지 않는다.

완료 기준:
- <사용자가 확인할 수 있는 동작>
- <로딩/빈 상태/에러 상태>
- <권한 또는 검증 규칙>

검증:
- <실행할 테스트/린트/타입체크 명령>
- 변경 파일과 남은 리스크를 보고한다.
```

## 좋은 요청 예시

- 합의된 비공개 데이터 계약을 기준으로 운영 목록 화면을 만든다.
- 식별자 정규화 검증을 추가한다.
- `operation_status`, `archive_status`, `session_duration_type` 계산 테스트를 작성한다.
- 기존 테이블 패턴을 재사용하도록 컴포넌트를 정리하되 동작은 바꾸지 않는다.

## 피해야 할 요청

- 대시보드 전체 만들어줘.
- 예쁘게 만들어줘.
- DB 알아서 연결해줘.
- 버그 다 고쳐줘.

## 작업 원칙

- 레이어별 작업보다 사용자 흐름이 완성되는 수직 단위 작업을 우선합니다.
- 작업 시작 전에 현재 브랜치, 최신 `dev`, 작업 범위, 관련 문서를 확인합니다.
- 작업을 중단하거나 이어받을 수 있게 현재 상태, 완료한 것, 남은 것, 실패한 명령을 남깁니다.
- 커밋은 생성 파일마다 하지 말고, 의미 있는 작업 단위가 끝났을 때 합니다.
- Claude Code가 DB 필드를 임의로 만들면 안 됩니다. 필드가 필요하면 먼저 비공개 데이터 계약을 확인합니다.
- 원천 데이터 구조가 화면 컴포넌트로 새어 나오면 안 됩니다. 반드시 표준 스키마로 매핑하는 repository/adapter를 둡니다.
- 원천 데이터 수집/적재는 staging/adapter를 거치고, 정규화 실패 데이터는 버리지 말고 검토 가능한 오류로 남깁니다.
- 실제 데이터 예시, 고객사명, 담당자명, 금액, 링크, 파일명, 원천 분석 문서는 공개 저장소에 커밋하지 않습니다.
- 실제 운영 데이터는 임의로 수정하거나 삭제하지 않습니다. 데이터 변경이 필요하면 백업, 문서화, 데이터 책임자 확인이 필요합니다.
- 운영 DB에 영향을 줄 수 있는 작업은 사용자가 백업 확인 여부와 실행 범위를 명시했는지 먼저 확인합니다.
- 삭제는 먼저 사용처를 확인하고, 실제 데이터는 물리 삭제하지 않습니다.
- 화면 문구는 운영자가 바로 이해할 수 있게 짧고 실무적으로 작성합니다.
- 화면 작업에는 로딩, 빈 상태, 에러 상태를 함께 고려합니다.
- 완료를 주장하기 전에 검증합니다.

## PR 안내 규칙

Claude Code는 PR을 만들고 끝내지 않습니다. 요청자가 PR 개념을 잘 몰라도 확인할 수 있도록, 무엇을 클릭하고 무엇을 확인해야 하는지까지 안내합니다.

PR 생성 후 반드시 아래 내용을 알려줍니다.

- PR 링크.
- 대상 브랜치가 `dev`인지 확인하라는 안내.
- 확인할 페이지 주소 또는 로컬 실행 주소.
- 화면에서 확인할 동작 목록.
- GitHub에서 `Files changed` 탭을 눌러 변경 파일을 보는 방법.
- 문제가 있으면 PR 댓글에 무엇을 남기면 되는지.
- 문제가 없으면 `Merge pull request` -> `Confirm merge`를 누르는 방법.
- `main`에는 직접 머지하지 말라는 경고.
- 검증 실패나 실제 데이터 수정이 있으면 머지하지 말라는 경고.

PR 생성 전에는 아래를 먼저 확인합니다.

- 앱이 실행되는지.
- 만든 화면이나 기능이 실제로 동작하는지.
- 콘솔이나 터미널에 명확한 에러가 없는지.
- 기존 화면이 깨지지 않았는지.
- 데이터 계약을 바꿨다면 비공개 계약 문서 또는 이슈에 남겼는지.
- 현재 PR 머지를 막는 미결정 사항이 남아 있는지.
- 실제 운영 데이터가 수정/삭제되지 않았는지.
- 가능한 검증 명령을 실행했는지.

PR 설명에는 아래 항목을 포함합니다.

- 변경 목적.
- 변경 파일.
- 확인한 동작.
- 실행한 검증 명령.
- 실패하거나 실행하지 못한 검증.
- 남은 리스크.
- 새로 추가한 미결정 사항과 PR 차단 여부.
- 데이터 계약, 스키마, 권한, 배포 설정 변경 여부.

## 커밋 메시지

커밋 메시지는 Conventional Commits 형식으로 작성합니다. 제목은 `feat: ...`, `fix: ...`, `docs: ...`, `chore: ...`, `refactor: ...`, `test: ...` 중 하나로 시작하고, 설명은 한국어로 씁니다. 제목은 `~한다` 같은 문장형보다 `feat: hub-om 협업 기준과 데이터 기반 확인`처럼 일반적인 커밋 요약형을 사용합니다.

본문에는 무엇을 바꿨는지보다 왜 바꿨는지, 어떤 제약이 있었는지, 무엇을 검증했는지, 남은 리스크가 무엇인지가 드러나야 합니다.

형식:

```text
<type>: <한국어 변경 요약>

<상세 설명: 문제 상황, 선택한 접근, 협업상 중요한 맥락을 2~5문장으로 작성>

제약: <작업에 영향을 준 제약>
거절한 대안: <검토했지만 선택하지 않은 대안> | <이유>
신뢰도: <낮음|중간|높음>
영향 범위: <좁음|보통|넓음>
검증: <실행한 테스트, 린트, 타입체크, 수동 확인>
미검증: <아직 확인하지 못한 부분>
```

`fix`, `update`, `작업`처럼 나중에 봐도 의미를 알 수 없는 제목은 쓰지 않습니다.
